# Demo video

`BallDarkEcho_Final_Demo.mp4` is the final gameplay demonstration for the submitted build.

The video is managed by Git LFS. If the file appears as a text pointer after cloning the repository, install Git LFS and run `git lfs pull`.
